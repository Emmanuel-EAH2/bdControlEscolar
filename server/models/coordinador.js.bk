const mongoose = require('mongoose');
let Schema = mongoose.Schema;

let coordiSchema = new Schema({

    nombre:{  
        type: String,
        required: [true, 'El nombre es un campo obligatorio']    
    },
    contraseña:{
        type: String,
        required: [true, 'La contraseña es estrictamente requeirda'],
        unique: true
    },
    repetir_contraseña:{
        type: Schema.Types.String,
        ref: 'contraseña'
    },
    secundaria:{
        type: String,
        required: [true, 'la secundaria es requerida']
    },
    estado:{
        type: Boolean,
        default: true
    }
});

module.exports = mongoose.model('Coordinador', coordiSchema);